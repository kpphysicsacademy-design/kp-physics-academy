document.addEventListener('DOMContentLoaded',function(){const main=document.querySelector('main.layout');if(!main)return;const cards=[...main.querySelectorAll('section.card')];const explore=main.querySelector('section.explore');const achievements=cards.find(s=>s.querySelector('h2')?.textContent.includes('Achievements'));if(explore&&achievements&&explore!==achievements){achievements.before(explore)}});